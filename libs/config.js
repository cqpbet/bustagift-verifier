
export const CHAIN_LENGTH = 1000000;

// We will use the hash of Bitcoin block #907975 as a salt. At the time of this commitment, that block has not yet been mined (verifiable via commit history).
export const PUBLIC_SECRET = "SALT_HERE";
